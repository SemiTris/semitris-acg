# 05-验收标准（框架搭建）

## 目录结构

- [ ] 顶层仅保留：`AGENTS.md`、`README.md`、`LICENSE`、`.gitignore`、`docs/`、`database/`、`backend/`、`frontend/`。
- [ ] `docs/` 内含：00~05 六类文档、设计可视化、`adr/`、`agent/`。
- [ ] `database/init.sql` 存在且与迁移前内容完全一致（`git diff` 验证）。
- [ ] 原 `backend/sql/` 与 `backend/semitris-acg/` 已不存在。

## AI 初始化

- [ ] 根目录存在 `AGENTS.md`，覆盖：项目定位、角色分工、工作流程、目录约定、关键技术契约、常用命令、安全红线。
- [ ] Claude Code 打开仓库后可直接据 `AGENTS.md` + `docs/agent/` + `docs/03-接口文档/` 开工，无需额外口头说明。
- [ ] `docs/agent/框架搭建/` 六件套（00~06）与 `docs/adr/0001-*` 就位。

## 引用一致性

- [ ] `README.md` 文档索引指向新路径（`docs/xxx`）。
- [ ] 全文检索不到指向旧路径的引用（`backend/sql`、顶层 `00-项目定义` 等）。

## 版本库

- [ ] git 历史保留（迁移显示为 rename 而非删除重建）。
- [ ] 无敏感文件（.env/密钥）进入版本库。
- [ ] 已提交并推送远程成功。
