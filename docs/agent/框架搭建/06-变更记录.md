# 06-变更记录（框架搭建）

## 变更清单

| 日期 | 变更 | 决策点 | 影响 | 回滚 |
|---|---|---|---|---|
| 2026-09-18 | 顶层六类文档目录迁入 `docs/` | 统一文档归口 | README/文档内路径引用需更新 | `git mv` 反向还原（git 历史保留） |
| 2026-09-18 | `设计可视化/` 迁入 `docs/设计可视化/` | 文档归口 | 可视化 HTML 路径变化 | 同上 |
| 2026-09-18 | `backend/sql/init.sql` → `database/init.sql` | 数据库脚本独立归口 | 数据库设计文档引用更新 | 同上 |
| 2026-09-18 | 新增根级 `.gitignore` | 覆盖 IDE/前端/.env/日志 | 不影响 backend/.gitignore | 删除文件即可 |
| 2026-09-18 | 新增项目级 `AGENTS.md` | AI 协作初始化（OpenCode 身份声明） | Claude Code 开工时读此初始化 | 按契约维护 |
| 2026-09-18 | 新增 `frontend/`（仅 README） | 前后端分离占位 | 后续搭建 Vite/Vue 工程 | 删除目录即可 |

## 未经变更事项（声明）

- 数据库 schema（5 表、外键级联、rating、BCrypt 等）**不变**。
- 接口契约（六大模块、R\<T\>、路径风格、分页）**不变**。
- 后端代码与命名规范**不变**。

## 后续更新

- Claude Code 实现各模块后，在本文档追加对应变更记录。
