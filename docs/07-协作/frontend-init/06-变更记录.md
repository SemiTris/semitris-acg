# 06-变更记录（frontend 前端）

## 变更清单

| 日期 | 事项 | 说明 |
|---|---|---|
| 2026-09-18 | 批次 A+B 实现完成（commit dcc6841） | `frontend/` 工程 + 公共层 + 卡片墙/编辑弹窗/删除；21 文件 |
| 2026-09-18 | 偏差登记：vite proxy 需剥离 `/api` 前缀 | 后端接口无 `/api` 前缀，rewrite 剥离后才通（F-01 实现的必要修正） |
| 2026-09-18 | 占位页：详情/追番/安利墙/登录四路由用 PlaceholderPage | 满足 F-04 可导航、不违反本批范围（批次 C 实现完整页） |
| 2026-09-18 | 批次 C 实现完成（commit 1176361） | 详情页（收藏/评价全链路）+ 追番列表 + 安利墙；api 三模块；StatusTag 增 kind=list；login 路由仍占位 |
| 2026-09-18 | 偏差登记（批次 C，可接受） | ① 详情页布局「左封面 200×280 + 右信息」自由实现；② 评价 content 可空（契约 rating 必填、content 可选，对齐正确）；③ 追番/安利展示进度数字、标签 split 逗号 |
| 2026-09-18 | 待人工验收 | 无头环境已做接口与构建验证；真实浏览器逐点击（收藏/评价/追番更新/安利增删改）留人工过一遍 |

## 本包声明

- 新建 `frontend/` 前端工程（Vue3 + Element Plus + Vite + axios + vue-router），无后端改动。
- 调用全部复用既有后端接口（docs/03-接口文档/），**不新增后端接口**。
- 前端无新增数据表；封面使用 OSS 直链。
