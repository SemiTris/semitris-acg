# 06-变更记录（recommend + file + review/page）

## 变更清单

| 日期 | 事项 | 说明 |
|---|---|---|
| 2026-09-18 | **file 模块改为阿里云 OSS 云存储**（替换本地磁盘方案） | 上传至 OSS `cover/{时间戳}.{扩展名}` 前缀，返回公开直链 URL；配置键 `oss.endpoint/access-key-id/access-key-secret/bucket-name` |
| 2026-09-18 | `docs/03-接口文档/file.md` 契约更新 | `/file/download` 弃用（OSS 直链后无本地回显），`/file/upload` 返回 OSS 直链 |
| 2026-09-18 | 配置安全：密钥不入库 | 新增 `application.example.yaml` 模板（oss.* 留空）；真实 `application.yaml` 由开发者复制填写；`backend/.gitignore` 排除 application.yaml 并解除既有跟踪 |
| 2026-09-18 | `pom.xml` 新增 OSS 依赖 | `aliyun-sdk-oss:3.17.4` + Java 9 所需 jaxb 三件套 |
| 2026-09-18 | 配置新增：`spring.servlet.multipart.max-file-size/max-request-size` | application.yaml 新增（默认 1MB 拒大图） |
| 2026-09-18 | `Review` 实体新增非持久化字段 title/cover | 供 review/page 连表，不影响 insert/update |
| 2026-09-18 | `review/page` 补做 | 原 collection-review 包范围声明，本包实现 |

（Claude Code 实现回报后在此补充实际变更。）

## 契约不变声明

- recommend 表结构、接口路径与返回符合 `docs/03-接口文档/recommend.md`。
- OSS 存储约定（cover 前缀、直链 URL、example 模板）已同步到 `docs/03-接口文档/file.md`。
- user_id 后端填充、连表 title/cover 非持久化、R&lt;T&gt; 等既有约定不变。
