# 06-变更记录（collection + review）

## 变更清单

| 日期 | 事项 | 说明 |
|---|---|---|
| 2026-09-18 | collection + review 后端实现完成（commit 8bec616） | 13 文件；9 接口逐条 HTTP 验证通过（含 rating 联动、删光置 NULL） |
| 2026-09-18 | 偏差登记（评审可接受，不改代码） | ① `@Alias("userCollection")` 保留（历史成因见 c7fb9ff，避免与 java.util.Collection 冲突）；② CollectionMapper.xml `resultType` 用全限定名（@Alias 存在时简单名不注册，技术必要）；③ title/cover 用普通字段非 transient（transient 会被 Jackson 忽略导致缺字段） |
| 2026-09-18 | 评审整改：editCollection progress 上限完整校验（commit 46eaae7） | 新增 CollectionMapper.selectCollectionById + XML；请求体缺 animeId 时查原记录取 anime_id 校验 `0<=progress<=episodes`，原记录缺失拒绝 |
| 2026-09-18 | 范围声明 | `review/page` 本期不做（属范围裁剪，非偏差），待推荐迭代实现 |

## 本包已登记的规范校准

- 2026-09-18：时间类型统一 `java.time.LocalDateTime`（与既有实体一致）。`docs/05-规范/01-编码规范.md` 第 6 条相应修订；存量 User 实体 `Date` 不迁移。
