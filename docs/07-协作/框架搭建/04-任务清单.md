# 04-任务清单（框架搭建）

## 本次任务（OpenCode 执行，已由人类确认）

| 任务ID | 目标 | 依赖 | 涉及文件 | 输入输出 | 验收 |
|---|---|---|---|---|---|
| S-01 | 迁移顶层文档目录至 docs/ | 无 | 00~05 六个目录 | 输入：原目录；输出：docs/00~05 | `git status` 显示 rename，无内容改动 |
| S-02 | 迁移 设计可视化 → docs/ | S-01 | 设计可视化/ | 同上 | 同上 |
| S-03 | 迁移数据库脚本 → database/ | 无 | backend/sql/init.sql | 输出：database/init.sql | 文件内容不变 |
| S-04 | 清理残留空目录 | S-03 | backend/semitris-acg | 删除 | 目录消失，无跟踪文件丢失 |
| S-05 | 生成根级 .gitignore | 无 | .gitignore（新建） | 输出：根忽略规则 | 覆盖 IDE/前端/.env/日志，不干扰 backend/.gitignore |
| S-06 | 生成项目级 AGENTS.md | 无 | AGENTS.md（新建） | 输出：AI 协作契约 | 含角色/流程/目录/契约/红线，OpenCode 与 Claude Code 可据其初始化 |
| S-07 | frontend 占位 | 无 | frontend/README.md | 输出：占位说明 | 目录就位，说明工程名/路由/风格来源 |
| S-08 | 更新交叉引用 | S-01~S-03 | README.md、数据库设计_优化版、Git与工程规范、计划书 | 输出：路径引用更新 | grep 无指向旧路径的引用 |
| S-09 | 记录决策 | S-01~S-08 | docs/adr/0001-*.md | 输出：ADR | ADR 覆盖背景/决策/影响/回滚 |
| S-10 | 提交与推送 | S-01~S-09 | git add/commit/push | 输出：约定式提交 | 单提交、无敏感文件、推送远程成功 |

## 后续任务（Claude Code 按文档包实现，另行出包）

- C-01 anime 模块：实体已有，补 Mapper+XML / Service / Controller（`docs/03-接口文档/anime.md`）。
- C-02 collection 模块：同上 + 连表 anime 返回 title/cover（collection.md）。
- C-03 review 模块：同上 + 事务内重算 anime.rating（review.md + 数据库设计优化版）。
- C-04 recommend 模块：同上 + 连表（recommend.md）。
- C-05 file 模块：上传/回显（file.md，保存到 `D:/upload/`）。
- C-06 user 登录接口：复用现有 User CRUD + BCrypt matches（user.md，可后置）。

> 以上 C-01~C-06 仅列出方向，正式开工需人类确认后由 OpenCode 出对应设计包或按现有契约直接派单。
